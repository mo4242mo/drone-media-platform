# ============================================
# 前端自动部署到 Azure Blob Storage
# ============================================
name: Deploy Frontend

on:
  push:
    branches:
      - main
    paths:
      - 'frontend/**'
      - '.github/workflows/deploy-frontend.yml'
  workflow_dispatch:  # 允许手动触发

env:
  AZURE_STORAGE_ACCOUNT: stdronemediastorage
  AZURE_CONTAINER: '$web'

jobs:
  deploy:
    runs-on: ubuntu-latest
    name: Deploy to Azure Blob Storage
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Upload frontend to Azure Blob Storage
        uses: azure/CLI@v2
        with:
          azcliversion: latest
          inlineScript: |
            az storage blob upload-batch \
              --account-name ${{ env.AZURE_STORAGE_ACCOUNT }} \
              --account-key "${{ secrets.AZURE_STORAGE_KEY }}" \
              --destination '${{ env.AZURE_CONTAINER }}' \
              --source ./frontend \
              --overwrite \
              --auth-mode key \
              --only-show-errors

      - name: Deployment Summary
        run: |
          echo "✅ Frontend deployed successfully!"
          echo "🌐 URL: https://${{ env.AZURE_STORAGE_ACCOUNT }}.z7.web.core.windows.net"
